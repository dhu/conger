<deploy>
    <publish stream="player_inits"      />

    <publish stream="player_positions"  />

    <publish stream="player_items"      />
    <publish stream="item_pickups"      />

    <publish stream="player_shots"      />
    <publish stream="player_damage"     />
    <publish stream="player_frags"      />
    <publish stream="player_deaths"     />

    <publish stream="map_change"        />
    <publish stream="mode_change"       />

    <publish stream="sound"             />
    <publish stream="chat"              />

    <publish stream="player_ping"       />

    <publish stream="world_edit"        />
    <publish stream="entity_edit"       />

    <publish stream="new_bots"          />

    <publish stream="client_disconnect" />
    <publish stream="reload_map"        />
    <publish stream="map_timeup"        />
    <publish stream="server_msgs"       />

    <node  endpoint=":15000" query="game_query"/>

</deploy>
