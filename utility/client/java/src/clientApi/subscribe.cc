#include <stdlib.h>
//#include "MedusaClient.h"

//using namespace Borealis;

//Status  output_handler( ptr<StreamEvent>  event ) { return string(""); };

int main(int argc, char* argv[])
{
/*
       int port = 40000;
        int dest_port = port + 10000;
       MedusaClient client(InetAddress("localhost", port ));
        cout << "subscription from localhost:" << port << " to localhost:" << dest_port << endl;

       Status  stat = client.set_data_handler(
                       InetAddress("localhost", dest_port),
                       wrap( &output_handler ));

       if ( stat )
       { 
          stat = client.fast_subscribe("borealis://brown.cs.brown.edu/test/output");

           client.run();
       }
*/
    return 0;
 }
